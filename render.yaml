services:
  - type: web
    name: dyk-training
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: DATABASE_URL
        value: "postgresql://postgres.dexyrfaoyhdudsamctqa:LavidaesbuenaDISFRUTA@aws-0-us-west-2.pooler.supabase.com:5432/postgres?sslmode=require"