services:
  - type: web
    name: dyk-training
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: DATABASE_URL
        value: "postgresql://postgres:TusNuevaContrasenaAqui@db.dexyrfaoyhdudsamctqa.supabase.co:5432/postgres?sslmode=require"